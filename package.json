{
	"name": "matcha",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"start": "concurrently \"npm run --prefix api nodemon\" \"npm run --prefix client start\""
	},
	"dependencies": {
		"bootstrap": "^4.5.0",
		"react-bootstrap": "^1.3.0"
	},
	"devDependencies": {
		"concurrently": "^5.2.0"
	}
}
